# DB-GUI API Environment Configuration

# App Database (stores checklists, instances, results)
DATABASE_URL=postgresql://user:password@localhost:5432/dbgui_app

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Note: In v1, targetDatabaseUrl is passed in request bodies.
# Future versions will use encrypted connection storage.
