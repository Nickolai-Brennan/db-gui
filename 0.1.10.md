## Step 0.1.10 — ESLint (root config + works in web/api/shared)

Goal: `pnpm lint` runs at root and enforces consistent rules everywhere.

---

# 0.1.10a) Install ESLint + TS + React plugins (root)

Run at repo root:

```bash
pnpm add -D eslint @eslint/js typescript typescript-eslint eslint-config-prettier \
  eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-import
```

---

# 0.1.10b) Create root ESLint config (flat config)

Create `/eslint.config.js`:

```js
import js from '@eslint/js';
import tseslint from 'typescript-eslint';
import importPlugin from 'eslint-plugin-import';
import reactPlugin from 'eslint-plugin-react';
import reactHooks from 'eslint-plugin-react-hooks';
import prettier from 'eslint-config-prettier';

export default [
  // Ignore outputs
  {
    ignores: ['**/dist/**', '**/node_modules/**', '**/coverage/**', '**/build/**'],
  },

  // Base JS recommended
  js.configs.recommended,

  // TS recommended (type-aware rules can come later)
  ...tseslint.configs.recommended,

  // Shared rules for all TS/JS files
  {
    plugins: {
      import: importPlugin,
    },
    rules: {
      ...prettier.rules,

      // Clean code
      'no-console': 'off', // allow for backend logging in v1
      'no-debugger': 'warn',

      // Imports
      'import/no-duplicates': 'error',
      'import/order': [
        'warn',
        {
          alphabetize: { order: 'asc', caseInsensitive: true },
          'newlines-between': 'always',
        },
      ],
    },
  },

  // React (web only) — we’ll scope by file path
  {
    files: ['apps/web/**/*.{ts,tsx}'],
    plugins: {
      react: reactPlugin,
      'react-hooks': reactHooks,
    },
    settings: {
      react: { version: 'detect' },
    },
    rules: {
      ...reactPlugin.configs.recommended.rules,
      ...reactHooks.configs.recommended.rules,
      'react/react-in-jsx-scope': 'off'
    },
  },
];
```

---

# 0.1.10c) Add root lint script (already present)

Your root `package.json` has `"lint": "pnpm -r lint"`.

Now we need each package to have a working `lint` command.

---

# 0.1.10d) Add `lint` scripts per package

## `packages/shared/package.json`

Replace the placeholder lint script with:

```json
"lint": "eslint ."
```

## `apps/api/package.json`

Replace the placeholder lint script with:

```json
"lint": "eslint ."
```

## `apps/web/package.json`

Add:

```json
"lint": "eslint ."
```

---

# 0.1.10e) Add `.eslintignore`? (not needed)

With flat config ignores, you can skip `.eslintignore`.

---

# 0.1.10f) Run lint

```bash
pnpm lint
```

If you hit React-specific lint errors due to Vite defaults, we’ll adjust rules—this is normal on first pass.

---

# Done criteria for 0.1.10

✅ `pnpm lint` runs from repo root
✅ Web lint includes React rules
✅ Api/shared lint includes TS rules
✅ dist/node_modules ignored

---

## Next Step 0.1.11 — EditorConfig (tiny but helpful)

If you say **continue**, I’ll provide the exact `.editorconfig` + quick add, then we go straight into **0.1.12 scripts polish** and **0.1.13 husky/lint-staged** (the real productivity boosters).
